{
  "name": "@squirreljs/squirrel-project-types",
  "version": "0.0.0",
  "license": "MIT",
  "sideEffects": false,
  "exports": {
    ".": {
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "//build": "pnpm unbuild",
    "//stub": "pnpm unbuild --stub",
    "clean": "pnpm rimraf .turbo node_modules dist",
    "lint": "pnpm eslint ."
  },
  "devDependencies": {
    "@squirreljs/squirrel-project-ts-config": "workspace:^0.0.0",
    "@squirreljs/squirrel-project-eslint-config": "workspace:^0.0.0",
    "unbuild": "^1.2.1"
  }
}
